<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.PostFavoriteMapper">
    
    <resultMap type="PostFavorite" id="PostFavoriteResult">
        <result property="id"    column="id"    />
        <result property="postId"    column="post_id"    />
        <result property="userId"    column="user_id"    />
        <result property="createdAt"    column="created_at"    />
    </resultMap>

    <select id="selectPostFavorite" resultMap="PostFavoriteResult">
        select id, post_id, user_id, created_at
        from post_favorites
        where post_id = #{postId} and user_id = #{userId}
        limit 1
    </select>
        
    <insert id="insertPostFavorite" parameterType="PostFavorite" useGeneratedKeys="true" keyProperty="id">
        insert into post_favorites
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="postId != null">post_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="createdAt != null">created_at,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="postId != null">#{postId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="createdAt != null">#{createdAt},</if>
         </trim>
    </insert>

    <delete id="deletePostFavoriteById" parameterType="Long">
        delete from post_favorites where id = #{id}
    </delete>

    <delete id="deletePostFavorite">
        delete from post_favorites where post_id = #{postId} and user_id = #{userId}
    </delete>

    <select id="selectPostFavoritesByUserId" parameterType="Long" resultMap="PostFavoriteResult">
        select id, post_id, user_id, created_at
        from post_favorites
        where user_id = #{userId}
        order by created_at desc
    </select>
</mapper>

